# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Env
.env
.env.local

# Node
node_modules/
dist/
build/
.next/

.claude
CLAUDE.md
docs/

# Hackathon API token — never commit
.ft-hack.json

# raw/*.json IS committed (not ignored): the Vercel deployment bundles it as
# static data for the serverless functions instead of live-fetching on every
# cold start. Synthetic, no-PII data per the hackathon brief. Re-derivable
# anytime via `npm run fetch -- --fresh`.

